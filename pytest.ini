[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    slow: network-dependent test (requires internet)
    network: requires internet to download model assets (huggingface.co)
