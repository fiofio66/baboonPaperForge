# ============================================================
# baboonPaperForge — Core Dependencies (Phase 1+)
# ============================================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.30
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
alembic>=1.13.0

# --- Validation & Settings ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# --- Security ---
cryptography>=42.0.0

# --- Agent Orchestration ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0

# --- Vector Database ---
pymilvus>=2.4.0

# --- LLM Providers (BYOK — install as needed) ---
# langchain-openai
# langchain-anthropic
# langchain-google-genai

# --- Document Parsing ---
pymupdf>=1.24.0           # PDF parsing (RAG ingestion)
python-docx>=1.1.0        # Word template parsing
TexSoup>=0.3.1            # LaTeX AST parsing (zero-token assembler)

# --- RAG Embeddings ---
sentence-transformers>=3.0.0  # Local embeddings (no API key)

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
aiosqlite>=0.20.0           # SQLite async driver for tests

# --- Search ---
ddgs>=7.0.0   # Web search (zero API key)

# --- Utilities ---
httpx>=0.27.0             # Async HTTP client (Search Agent downloads)
aiofiles>=24.1.0          # Async file I/O
rich>=13.0.0              # Fancy terminal logging
tenacity>=8.5.0           # Retry decorator
